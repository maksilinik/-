# 🏦  Банковский сервис на Go

Сервис для управления банковским счетом с поддержкой операций пополнения, снятия, переводов и истории транзакций.

## 📋 Возможности

✅ **Пополнение счета** (Deposit) - с проверкой суммы  
✅ **Снятие средств** (Withdraw) - с проверкой баланса  
✅ **Переводы** (Transfer) - между счетами  
✅ **Получение баланса** - текущее состояние счета  
✅ **Выписка по счету** - детальная история транзакций  
✅ **Валидация операций** - проверка корректности данных  

## 🏗️ Архитектура

### Структура проекта
```
bankapp/
├── models/          # Модели данных
├── interfaces/      # Интерфейсы
├── services/        # Реализации сервисов
└── errors/          # Кастомные ошибки
```

### Основные компоненты

#### 1. Модели (models)
- **Account** - банковский счет
- **Transaction** - транзакция
- **TransactionType** - тип операции

#### 2. Интерфейсы (interfaces)
- **AccountService** - операции со счетом
- **Storage** - хранилище данных

#### 3. Сервисы (services)
- **AccountServiceImpl** - реализация операций

## 🚀 Использование

### Инициализация сервиса
```go
import "bankapp/services"

// Создание сервиса
account := &models.Account{
    ID:        "ACC123",
    OwnerName: "Иван Иванов",
    Balance:   1000.00,
}

storage := // инициализация хранилища
service := services.NewAccountService(account, storage)
```

### Базовые операции

#### Пополнение счета
```go
err := service.Deposit(500.00)
if err != nil {
    fmt.Println("Ошибка:", err)
}
```

#### Снятие средств
```go
err := service.Withdraw(200.00)
if err != nil {
    switch err {
    case errors.ErrInsufficientFunds:
        fmt.Println("Недостаточно средств")
    case errors.ErrInvalidAmount:
        fmt.Println("Неверная сумма")
    }
}
```

#### Перевод между счетами
```go
toAccount := &models.Account{
    ID:   "ACC456",
    // ... другие поля
}

err := service.Transfer(toAccount, 300.00)
if err != nil {
    fmt.Println("Ошибка перевода:", err)
}
```

#### Получение баланса
```go
balance := service.GetBalance()
fmt.Printf("Текущий баланс: %.2f\n", balance)
```

#### Получение выписки
```go
statement := service.GetStatement()
fmt.Println(statement)
```
